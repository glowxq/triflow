spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:triflow123}
      database: ${REDIS_DATABASE:6}
      timeout: 50000
      jedis:
        pool:
          max-active: 100
          max-wait: -1
          max-idle: 30
          min-idle: 10

redis:
  listener:
    enable: true
